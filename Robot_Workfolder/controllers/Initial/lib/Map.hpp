#pragma once

#include <string>
#include <fstream>
#include <vector>
#include <utility> // std::pair
#include <stdexcept> // std::runtime_error
#include <sstream> // std::stringstream

class Map
{
private:
    /* data */
public:
    Map(/* args */);
    ~Map();
    void Read_Map();
};

Map::Map(/* args */)
{
}

Map::~Map()
{
}
